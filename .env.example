# Optional but recommended: raises GitHub API rate limit from 60/hr to 5,000/hr
# and lets you sync private repos you have access to.
# Create one at https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=
